import os
os.rename("bankingt_seq_8622.in","bankingt_seq_8622.txt")

# import re

# def convert_to_space_separated(text):
#     return re.sub(r'(?<!^)([A-Z])', r' \1', text)

# # Read the file
# input_file = "snips_label_13084.txt"
# output_file = "snips_label_formatted.txt"

# with open(input_file, "r") as f:
#     lines = f.read().splitlines()

# # Convert each label
# converted_lines = [convert_to_space_separated(line) for line in lines]

# # Write the output to a new file
# with open(output_file, "w") as f:
#     f.write("\n".join(converted_lines))

# print(f"Conversion complete! Output saved to {output_file}")